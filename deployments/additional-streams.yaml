apiVersion: apps/v1
kind: Deployment
metadata:
  name: traffmonetizer-node
  labels:
    app: traffmonetizer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: traffmonetizer
  template:
    metadata:
      labels:
        app: traffmonetizer
    spec:
      containers:
      - name: traffmonetizer
        image: docker.io/traffmonetizer/cli_v2:latest
        args: ["start", "accept", "--token", "$(TRAFFMONETIZER_TOKEN)"]
        env:
        - name: TRAFFMONETIZER_TOKEN
          valueFrom:
            secretKeyRef:
              name: passive-revenue-secrets
              key: TRAFFMONETIZER_TOKEN
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: earnfm-node
  labels:
    app: earnfm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: earnfm
  template:
    metadata:
      labels:
        app: earnfm
    spec:
      containers:
      - name: earnfm
        image: docker.io/earnfm/client:latest
        env:
        - name: EARNFM_TOKEN
          valueFrom:
            secretKeyRef:
              name: passive-revenue-secrets
              key: EARNFM_TOKEN
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: repocket-node
  labels:
    app: repocket
spec:
  replicas: 1
  selector:
    matchLabels:
      app: repocket
  template:
    metadata:
      labels:
        app: repocket
    spec:
      containers:
      - name: repocket
        image: docker.io/repocket/repocket:latest
        env:
        - name: RP_EMAIL
          valueFrom:
            secretKeyRef:
              name: passive-revenue-secrets
              key: HONEYGAIN_EMAIL
        - name: RP_API_KEY
          valueFrom:
            secretKeyRef:
              name: passive-revenue-secrets
              key: REPOCKET_TOKEN
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
